# Skill: <name>

## Description

One sentence. What does this skill do?

## Usage

```text
/invoke-skill <name> <args>
```

Or from code: `Skill("name", args={...})`

## Requirements

- Tool A
- Tool B

## Self-check

```bash
# One command that proves this skill works
```

Fails if the skill is broken. Passes silently if all good.
